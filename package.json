{
  "name": "sigilcraft",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "SIGILCRAFT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.worker.json --noEmit",
    "start": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260729.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "wrangler": "^4.115.0"
  }
}
